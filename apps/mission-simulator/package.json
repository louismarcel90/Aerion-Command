{
  "name": "@aerion/mission-simulator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@aerion/assurance": "workspace:*",
    "@aerion/contracts": "workspace:*",
    "@aerion/domain": "workspace:*",
    "@aerion/event-bus": "workspace:*",
    "@aerion/flight-model": "workspace:*",
    "@aerion/mission-engine": "workspace:*",
    "@aerion/renderer-ascii": "workspace:*",
    "@aerion/scenario-kit": "workspace:*",
    "@aerion/sensor-engine": "workspace:*",
    "@aerion/simulation-kernel": "workspace:*",
    "@aerion/state-store": "workspace:*",
    "@aerion/threat-engine": "workspace:*",
    "@aerion/weapon-engine": "workspace:*",
    "@aerion/replay-engine": "workspace:*",
    "@aerion/evidence": "workspace:*"
  },
  "scripts": {
    "dev": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest --passWithNoTests",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  }
}